# two_moons

three-quarter 円を 2 つ噛み合わせた、定番の 2 値分類データ。標準ライブラリだけで
生成するので依存が要らない。

| 引数 | 意味 |
| --- | --- |
| `split` | `train` / `val`。**trainer が実行時に渡す**ので YAML には書かない |
| `n` | 標本数 |
| `noise` | ガウスノイズの標準偏差 |
| `seed` | 生成の種 |

`n_classes` を属性として公開する。model の `k` はここから実行時に決まる。

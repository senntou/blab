[project]
name = "blab"
version = "0.2.0"
description = "実験を、再現できるコードの木として記録する実験管理ツール"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = ["pyyaml>=6"]

[project.optional-dependencies]
ui = ["fastapi>=0.110", "uvicorn>=0.29"]
parquet = ["pyarrow>=15"]
dev = ["pytest>=8", "httpx>=0.27"]

[project.urls]
Repository = "https://github.com/senntou/blab"

[project.scripts]
blab = "blab.cli:main"

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["blab"]

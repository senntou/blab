# What

実験管理ツールを作るので、その仕様を詰めます。

名前はALab

# Requirements

- 実験をrunとして保存ができる
- 保存されるディレクトリ構造とrunの階層構造が一致している
- UIと保存は独立に
    - 保存自体は、ただ指定のディレクトリ構造に保存をするだけ
    - それに対して、保存方法が決まったテンプレートに沿っていれば、UIで閲覧などの処理ができるようにする
- クエリを叩いて実験比較ができると良さそう
    - parquetを使って情報を保存しておくのが良さそうか？
- UIでも実験比較はしたい
    - ハイパラなどの差分比較
    - 結果の比較（グラフへのプロット・表にするなど）
    - アーティファクトを並べて比較
- CVを管理できる階層構造
    - runをグルーピングして、その中で結果（テスト評価値）を集計できる機能
- 階層構造についてまとめると、いかができていれば良い
    - Experimentsの下にRunがある階層構造
    - Runとは別に、ExperimentsにはGroupも登録可能
    - Groupには複数のRunが属する
    - Group構造も、ディレクトリ構造に反映される
    - こんな感じ
    
    ```
    Experiment
     ├── Run
     ├── Run
     ├── CV
     │    ├── Run
     │    ├── Run
     │    └── Run
     └── ...
    ```
    
- 保存する情報について（例）
    - lossやmetricの、各epochや各stepでの値
        - これは配列構造になる
    - test後の値
        - これは1 runにつき1つになる
    - 実行時間や実行時刻、実験の名前やid
    - ハイパラなどの実行時情報
    - アーティファクト（その他のファイルを自由に。最低限、画像などはUIで閲覧したい）

# Tech Stack

- UIは HTML/CSS で実装する
- JSも併用する
- 保存レイアウトを統一するためのパッケージを自作する。
